name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  commits:
    name: Conventional commits
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Validate commit messages
        run: |
          pattern='^(feat|fix|docs|ci|chore|refactor|test|perf|build|style)(\(.+\))?(!)?: .+'
          base=${{ github.event.pull_request.base.sha }}
          for sha in $(git rev-list "$base"..HEAD); do
            msg=$(git log --format='%s' -1 "$sha")
            if echo "$msg" | grep -qE '^Merge '; then
              continue
            fi
            if ! echo "$msg" | grep -qE "$pattern"; then
              echo "::error::Bad commit message: $msg"
              echo "Expected format: type(scope): description"
              exit 1
            fi
          done

  test:
    name: Test (Go ${{ matrix.go-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        go-version: ["1.22.x", "stable"]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}
      - run: go build ./...
      - run: go test -race -count=1 ./...
      - run: go vet ./...

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: false
      - uses: golangci/golangci-lint-action@v9

  all-checks:
    name: All checks
    if: always()
    needs: [commits, test, lint]
    runs-on: ubuntu-latest
    steps:
      - name: Decide status
        env:
          JOBS_HAVE_FAILURES: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
        run: |
          if [ "$JOBS_HAVE_FAILURES" = "true" ]; then
            echo "One or more jobs failed"
            exit 1
          fi
